import { getDB } from "../../database";

export async function addNewProduct(
  title: string,
  description: string,
  price: number,
  sellingPrice: number,
  categoryId: number,
  discountId: number
) {}
